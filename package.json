{
  "name": "timesheet-ess",
  "version": "1.0.0",
  "description": "PulseSheet is a lightweight timesheet MVP that runs entirely in the browser. It is designed as a fast starting point for a real product: time entry, weekly visibility, status tracking, filtering, and CSV export are already wired in.",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
